<%= @post.title %>
<%= render :partial => "part_content" %>
  <ol>
    <% for comment in @comments -%>
      <li><%= comment.name %><br/><%= comment.body %></li>
    <% end -%>
  </ol>
<br/>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %> | <%= link_to 'Add Comment', "/comments/new/#{@post.id}" %>